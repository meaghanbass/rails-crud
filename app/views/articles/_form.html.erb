<%= form_with model: @article, local: true do |form| %>
  <%= form.label :title %><br>
  <%= form.text_field :title %>

  <%= form.label :text %><br>
  <%= form.text_area :text %>

  <%= form.submit class: 'bg-purple-500 hover:bg-purple-700 text-white font-bold py-4 px-8 rounded' %>
<% end %>